<UserControl x:Class="KretaBasicSchoolSystem.Desktop.Views.LeftPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KretaBasicSchoolSystem.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <Border CornerRadius="10,0,0,10" Grid.Column="0">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.7">
                <GradientStop Color="{StaticResource backgroundPrimaryColor1}" Offset="0"/>
                <GradientStop Color="{StaticResource backgroundPrimaryColor2}" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <StackPanel Orientation="Vertical"                     
                    Margin="0,0,0,0">
            <!-- Logo -->
            <Image Margin="0 30 0 0" Source="/Images/kreta_atl.png" Height="40"/>
            <!-- Menu buttons -->
            <local:Menu Margin="0 20 0 0"/>
        </StackPanel>
    </Border>
</UserControl>
